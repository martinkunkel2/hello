main_workflow:
  steps:
    - branch_package:
        source_project: home:martinkunkel:github
        source_package: obs-scm-test
        target_project: home:martinkunkel:github:CI
  filters:
    event: pull_request
rebuild_master:
  steps:
    - rebuild_package:
        project: home:martinkunkel:github
        package: obs-scm-test
  filters:
    event: push
    branches:
      only:
        - master
